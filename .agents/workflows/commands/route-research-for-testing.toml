description = "수정된 라우트 매핑 및 테스트 실행"

prompt = """
## 컨텍스트

이번 세션에서 변경된 라우트 파일(자동 생성):

!{cat "${GEMINI_PROJECT_DIR:-$PWD}/.agents/workflows/tsc-cache"/*/edited-files.log | awk -F: '{print $2}' | grep '/routes/' | sort -u}

사용자가 추가로 지정한 라우트: {{args}}

## 할 일

아래 번호 순서를 **그대로** 따르세요:

1. 자동 목록과 {{args}}를 합친 뒤, 중복 제거(dedupe)하고 `src/app.ts`에 정의된 모든 프리픽스(prefix)를 해석/적용합니다.
2. 최종 라우트 각각에 대해, 경로(path), 메서드(method), 예상 요청/응답 형태(shape), 유효/무효 페이로드 예시를 포함하는 JSON 레코드를 출력합니다.
3. 아래를 사용해 **이제 `Task` 도구를 호출**합니다:

```json
{
    "tool": "Task",
    "parameters": {
        "description": "route smoke tests",
        "prompt": "Run the auth-route-tester sub-agent on the JSON above."
    }
}
```
"""
