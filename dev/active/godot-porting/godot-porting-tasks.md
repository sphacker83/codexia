# DungeonNeko Godot Native Full Rewrite Tasks

Last Updated: 2026-03-12

## 문서 운영

- active 진행 항목과 parked 후속 phase만 유지한다.
- 참조문서, 테스트 결과물, 테스트 하네스 기록은 active 작업면에서 제거했다.
- 메인 맵 로딩 문제는 사용자 지시로 차후 과제로 남기고, 이번 `Phase 6` 완료 범위에서 제외한다.
- `폐광촌` 외 다른 맵 로딩 시도는 하지 않는다.

## Phase 6. Script/UI/Content

- [x] 테스트/참조 정리
  - `dev/active/godot-porting/reference/*` 제거
  - `dev/active/godot-porting/evidence/*` 제거
  - `scripts/tests/*`, `scenes/tests/*`, smoke/pipeline 결과물 제거
  - `AppRoot` smoke 부팅 훅과 `GameScene/MenuScene/TitleScene/GreenfieldWorldRuntime` 테스트 API 제거
  - 상단 `Rewrite Status` 오버레이 제거
  - HUD `플레이 상태` summary card 제거
- [~] 메인 맵 로딩 parity
  - 사용자 지시로 차후 과제로 보류한다.
  - `map 310 -> requested 151 -> resolved 300` fallback은 이번 `Phase 6` 완료 범위에 넣지 않는다.
  - 사용자 요청으로 world follow camera를 전역 `hero always center` 규칙으로 바꿨다.
  - town/dungeon 공통으로 follow camera clamp를 제거하고, hero 월드 좌표가 항상 viewport 정중앙에 오도록 유지한다.
- [x] 모바일 export 부팅 로더 안정화
  - `DataRegistry` generated resource 인덱싱을 `DirAccess` 재귀 열거 + `Godot.FileAccess` 읽기로 교체해 encrypted Android/iOS export에서도 `res://` runtime content scan이 깨지지 않도록 정리했다.
  - `DataManager.LoadAllDataAsync()`의 `Task.Run + System.IO` 절대경로 로딩을 제거하고, 아이템/몬스터/NPC/퀘스트/스킬을 모두 Godot API 기준으로 읽도록 통일했다.
  - warmup/data load 실패 원인은 `DataRegistry.LastWarmupError`, `DataManager.LastLoadError`, `GodotRuntimeBootstrap.LastFailureMessage`로 연결하고, `LoadingScene`에 고정 실패 문구로 노출되도록 정리했다.
  - Android 릴리스 preset 암호화는 유지하고, 비교용 `Android Debug Unencrypted` preset을 추가했다.
  - 후속 수정으로 실제 Android export 자산이 `.tres` 대신 `.tres.remap`만 노출되는 구조를 반영해, generated map spec / tileset / sprite spec 인덱싱이 `.remap` 엔트리에서도 정상 등록되도록 보강했다.
- [x] 상단 UI parity
  - HUD 빠른 메뉴에 미니맵 토글과 현재 상태(`OFF/소/대`)를 연결하고, 핫키 토글 후 HUD 상태도 즉시 동기화
  - `docs/tbar.png` 레퍼런스 기준으로 상단 top bar를 전용 패널/전용 버튼 렌더러로 재작성하고, 기존 공용 `StyleBoxFlat` 빠른 메뉴 코드는 제거
  - `추격섬멸` / `표시 HQ` 버튼과 코드 경로를 제거하고, 전투 토글을 `수동전투` / `근접대응` 2상태로 단순화
  - `레거시/고품질`을 월드 필터가 아니라 runtime sprite composition quality로 교정하고, 상단 UI 상태를 전역 로컬 설정으로 복원
  - 상단 UI 실제 버튼 폭을 `BGM` 기준으로 통일하고, 안티 설정은 단일 토글 버튼, 우측 가이드는 `인벤토리` 버튼으로 재구성
  - 안티 ON에서 ground tile layer를 `Linear`로 적용하고, generated map tileset은 `UseTexturePadding = true` 기준으로 재생성
  - `폐광촌` 플레이 경로 기준 남은 control/state gap closeout 완료
- [x] HUD 상태 parity
  - 상단 UI와 함께 최종 status/control 표현 정리
  - 상점 특수 구매(`랜덤 장비/재료`, `HP 포션`, `EXP`) 메뉴와 실행 액션 연결
  - `ShopCatalogSnapshot` 가격값을 실제 런타임 값으로 채워 상점/재감정 비용 0 표시 제거
  - HUD 패널 메시지를 내부 action/warning 코드 대신 한국어 사용자 메시지로 변환
  - status card / quickslot / potion / character inventory status에 남아 있던 영어 placeholder와 raw `combat=*` 요약을 한국어 사용자 문구로 정리
  - status card를 compact layout으로 줄여 HP/MP/EXP 영역이 미니맵을 가리지 않도록 정리
  - 미니맵 위치를 고정 `BaseTop`이 아니라 HUD status card 실제 하단 + 여백 기준으로 재배치해 해상도/최소크기 변화에서도 겹치지 않도록 수정
  - status card 폭/높이/게이지 폭과 내부 폰트를 다시 줄여 좌상단 점유면적을 한 번 더 축소
  - `폐광촌` 플레이 경로 기준 최종 status/control 표현 정리 완료
- [~] 던전 runtime parity
  - 생성 규칙, 추적, 공격거리, 피격/상태이상, 복귀 흐름 정리
  - `Paralyze`가 상태 표시만 남고 실제 입력을 막지 않던 문제를 수정해서 이동/공격/스킬 캐스트를 차단
  - 영웅 공격/스킬 타깃 해석이 맵 전체 nearest monster fallback으로 새던 문제를 수정해서 전방/근거리 범위로 제한
  - 전방 타깃 탐색이 벽/막힌 타일을 무시하던 문제를 수정해서 장애물 뒤 몬스터를 타깃하지 않도록 정리
  - Godot 전용 divergence로 기본공격/NPC 상호작용/monster pending impact 재검사를 타일 기준에서 픽셀 기준으로 전환
    - `GreenfieldWorldRuntime`에 `TryResolveHeroAttackTarget`, `TryResolveHeroAttackDirection`, `CanMonsterAttackHeroNow`, `TryResolveNearestNpcInteractionTarget`를 추가하고 `CollisionBoxes + Entry.Position + Rect2.Intersects`를 공용 hit-test 축으로 사용
    - `GameScene` 기본공격 방향 보정/히트 판정, NPC 선택, monster pending impact 취소 재검사를 새 runtime API로 교체
    - monster AI 공격/추격과 NPC 근접/시선 판정도 world anchor 픽셀 기준으로 바꾸고, 이동만 foot tile 규칙 유지
  - `DungeonMapRenderer2D` depth를 `band + row stride + tileX tie-break`로 재정리해서 맵/오브젝트/엔티티 레이어가 row ordering에 눌리지 않도록 수정
  - 미스테리 던전 몬스터 pool/type-count/총 등장 수를 `mysteryDungeonRules.ts` 기준(5~10종, 총 100)으로 맞추고, 개별 스폰도 템플릿 순환 대신 pool 랜덤 선택으로 정리
  - 미스테리 던전 몬스터 스폰을 중앙 고정 배치 대신 walkable tile shuffle 기반으로 옮겨 네코웹 spawn gate 규칙에 더 가깝게 정리
  - `ActiveNpcObject` / `ActiveMonsterObject`가 렌더러에서 통째로 빠지던 누락을 보완해서, `active_obj_cnt/dir` 기반 맵 활성 오브젝트가 다시 top layer에서 순환 이동/애니메이션되도록 포팅
  - 미스테리 던전 `gate=2` / `gateIndex==0 귀환` 임시 규칙을 제거하고, web처럼 `gate=1` + `mystery_sacred_stone` 획득 전 하강 게이트 잠금 흐름으로 맞춤
  - `guide_data(x/y/px/py/dir)` compat 값을 runtime spec까지 유지하고, 성석 sprite key/좌표를 runtime spec과 world runtime overlay preview까지 전달
  - 성석 pickup 시 overlay preview 제거와 minimap/runtime spec 동기화까지 반영
  - gate guide는 `COMMONSPR_spr_0` overlay preview wiring에 더해 `DataRegistry` synthetic binding fallback과 `guide_data 우선 / gate tile,pixel fallback` 좌표 해석까지 반영했다. 실제 in-engine 시각 확인은 아직 남아 있다.
  - 성석 sprite key가 실제 로컬 asset index에 없을 때도 pickup logic이 깨지지 않게 타일 상태를 유지하고, 가용한 근접 sprite key로 fallback 하도록 보강
  - `gate -> reward popup -> 450ms -> floor reload` 흐름을 web처럼 다시 붙이고, popup 본문에 `EXP/GOLD/처치 몬스터/보상 아이템` 요약을 표시
  - `MoveDungeonDown` 보상 계산도 단순 선형식 대신 web `rollServerStyleReward` 기준으로 바꿔 floor clear `EXP/GOLD/보상 장비`를 산출하고 실제 인벤토리에 지급
  - 던전 돌파 popup의 보상 아이템 목록이 더 이상 빈 껍데기가 아니고, 인벤토리 만석 경고도 web 규칙과 같은 위치에서 결정
  - 몬스터 처치 즉시 `EXP/GOLD` 반영과 15% 서버식 장비 드롭도 다시 붙여서, floor clear popup 아이템 목록에 `층 이동 보상 + 층내 드롭`이 함께 집계되도록 정리
  - level-up gameplay message effect는 몬스터 처치 보상 경로와 mystery dungeon 층 이동 보상 경로 모두 `ShowHeroLevelUpPresentation()` 호출로 연결했다.
  - `폐광촌` mystery dungeon 플레이 경로 기준 closeout 선언은 false closeout으로 되돌린다.
  - 확정 차이:
    - 이전 discrepancy log에서는 mystery dungeon down-gate guide가 `map=903 ... guide=0 sacred=0`으로 끝났고, 이번 세션에서 binding fallback + `guide_data` 우선 좌표 보정까지 반영했다.
    - combat presentation은 코드상 보정이 들어갔지만, 실제 mystery runtime에서 guide / hero-hit / monster-hit 체감 parity의 수동 확인이 아직 남아 있다.
- [x] `CHSCRIPT` / `CHGAME_SCRIPT` / quest / quickslot / blacksmith parity
  - 잔여 workflow와 저장 의미론 정리
  - quest 진행/보상 메시지에서 raw code(`quest_turnin_*`, `quest_script:*`) 노출 제거
  - quest 패널이 전체 카탈로그 대신 실제 활성/완료 퀘스트 기준으로 보이도록 정리
  - quickslot 해제 메시지가 절대 슬롯 인덱스 대신 사용자 슬롯 번호로 보이도록 정리
  - blacksmith/shop/quest reward warning 중 raw code로 남던 `blacksmith_plus_*`, `blacksmith_repower_material_missing`, `shop_buy_item_not_in_catalog`, `*_inventory_full` 경고를 한국어 메시지로 정리
  - quickslot / quest tracker / economy-save 경로를 재점검해 `폐광촌` 플레이 경로 기준 저장 의미론 잔여 blocker 없음 확인
  - `폐광촌` 플레이 경로 기준 workflow/save closeout 완료

## 수용 기준

- [x] 상단 UI/HUD가 placeholder 없이 실제 플레이 경로에 맞게 동작한다.
- [~] 던전 runtime이 실제 규칙 기준으로 유지된다.
- [x] 테스트 API와 참조문서 의존 없이 `Phase 6` 작업면이 유지된다.
- [~] 메인 맵 보류 상태와 무관하게 `폐광촌` 플레이 경로 기준 `Phase 6` 잔여 gap이 닫힌다.

## 현재 상태

- `Phase 6` closeout은 다시 열린 상태다.
- `폐광촌` mystery dungeon 기준으로도 guide와 일부 combat presentation parity의 최종 수동 확인이 남아 있다.
- HUD compact/minimap safe area, renderer depth, spawn tile shuffle 등 일부 보정은 들어갔지만, 최신 확정 차이 때문에 던전 runtime closeout은 유지하지 않는다.
- 이번 턴에서는 `dotnet build DungeonNeko-Godot/DungeonNeko.csproj`와 rendered manifest/clip manifest 정적 검증만 완료했고, mystery dungeon guide parity의 실제 in-engine 수동 확인은 아직 하지 않았다.
- 이번 세션 추가 검증:
  - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS (`warnings 0`, `errors 0`)
- 메인 맵 로딩 parity는 사용자 지시대로 별도 차후 과제로 유지.

## Phase 7. 미적용 이펙트 / UI 재작성

- [~] 7-1. 캐릭터 상태이상 표시 복구
  - [x] 7-1-1. HUD 상태이상 데이터 계약 확장
    - `HudStatusSnapshot`에 `HasCondition`, `ConditionLabel`, `ConditionDetailText`, `IsConditionBlocking`을 추가했다.
    - `CombatSummary`는 일회성 전투 로그로 유지하고, persistent 상태표시는 별도 계약으로 분리했다.
  - [x] 7-1-2. `GameScene` 상태이상 브리지 정리
    - `_heroCondition`, `_heroConditionRemainingSec`, `_heroConditionDamage`를 HUD snapshot으로 보내도록 연결했다.
    - town/dungeon/combat 전환과 hero reset 경로에서도 `SyncHeroConditionPresentation()`이 호출되도록 정리했다.
  - [x] 7-1-3. HUD status card 상태이상 표시 추가
    - status card에 persistent 상태이상 row를 추가했다.
    - `중독/마비/빙결/화상` 색상 규칙을 HUD helper로 고정했다.
  - [x] 7-1-4. CharacterInventoryOverlay 상태이상 parity
    - character inventory status 영역도 HUD와 같은 상태이상 source를 재사용하도록 연결했다.
    - HUD와 overlay가 같은 문구/색 helper를 쓰도록 맞췄다.
  - [x] 7-1-5. 캐릭터 머리 위 상태이상 애니메이션 복구
    - hero follow transient effect 경로를 추가하고, `commonspr:10/15/16` 기반 condition effect를 상태이상 지속 중에 머리 위에 붙였다.
    - 상태이상 종료, 맵 재적용, hero reset 시 effect cleanup도 같이 정리했다.
  - [~] 7-1-6. 상태이상 표시 acceptance 고정
    - 현재 구현 기준:
      - HUD/overlay는 상태이상 유무와 남은 시간, 독 피해, 행동 차단 여부를 상시 표시한다.
      - hero/monster condition effect 계약은 `POISON/STUN/CONFUSE/NO_POTION/NO_SKILL/ICE/PARALYZE/SLOW/FIRE`까지 확장했고, `commonspr:10/13/14/15/16`와 `atbeffspr:3` 매핑을 반영했다.
      - standalone effect sprite binding 누락은 `DataRegistry` + `SpriteRuntimeBindingRegistry` synthetic binding fallback으로 보강했고, `commonspr:0/7/10/13/14/15/16`, `atbeffspr:3`, `maspr:0` 대상 정적 검증을 끝냈다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS.
    - 다만 mystery dungeon 실플레이에서 각 상태의 위치/지속시간/체감이 네코웹과 같은지는 수동 확인이 남아 있어 open 상태를 유지한다.

- [~] 7-2. 던전 하강 통로 이펙트 parity 복구
  - [x] 7-2-1. 던전 하강 통로 이펙트 기준 시각 정리
    - 네코웹 기준으로 별도 신규 asset이 아니라 `COMMONSPR_spr_0` mystery down gate guide preview 자체가 활성화 시 `scale + alpha` pulse를 반복하는 구성이 기준임을 고정했다.
    - 따라서 Godot에서도 `guide preview`는 유지하고, 누락된 것은 활성 state pulse effect라는 기준으로 정리했다.
  - [x] 7-2-2. down passage effect 누락 원인 분리
    - 실제 직접 원인은 세 가지였다.
      - `GreenfieldWorldRuntime`가 `COMMONSPR_spr_0`, `HSPR_1_spr_5` 같은 web 표기 문자열을 그대로 써서 Godot `DataRegistry` key(`commonspr:0`, `hspr_1:5`) lookup에 실패했고, preview 자체가 spawn되지 않았다.
      - standalone effect sprite가 owner binding 생성 대상에서 빠져 있어 `commonspr:*` / `atbeffspr:*` / `maspr:*` lookup 자체가 비는 구조였다.
      - mystery down gate 선택도 `next_map == 303`만 보던 상태라, 해당 gate가 없는 runtime map에서는 guide preview 기준점이 비는 경우가 있었다.
    - 현재는 normalized sprite key, synthetic binding fallback, `guide_data 우선 / gate tile,pixel fallback` 좌표 해석까지 반영했다.
  - [x] 7-2-3. 던전 gate/effect 레이어 재정의
    - 현재 overlay preview 레이어를 유지하고, 하강 통로 이펙트도 같은 preview에 tween만 추가하는 방향으로 정리했다.
    - 별도 레이어 추가 없이 기존 depth 체계를 보존한다.
  - [x] 7-2-4. down passage effect spawn/cleanup wiring
    - `_mysteryDownGateGuideTween`를 추가해 gate guide preview 생성 후 활성 상태에서 반복 tween이 시작되고, gate 비활성/맵 재적용/preview clear 시 즉시 정리되도록 연결했다.
    - floor reload 이후 잔상/중복 tween이 남지 않도록 cleanup 경로를 같이 묶었다.
  - [~] 7-2-5. gate 상태와 effect 동기화
    - 설계 기준은 `성석 미보유 = 정적`, `성석 획득 후 = pulse`로 정리돼 있다.
    - 이번 턴에서는 build/static verification만 완료했고, guide spawn/visibility의 실제 runtime 확인은 아직 다시 하지 않았다.
  - [~] 7-2-6. 하강 통로 이펙트 acceptance 고정
    - acceptance는 false closeout으로 되돌린다.
    - binding fallback과 `guide_data` 우선 좌표 보정은 반영됐지만, pulse guide/effect acceptance는 실제 in-engine 수동 확인 전까지 열어 둔다.
  - [~] 7-2-7. 몬스터 등장 / 미니맵 상태 유지
    - `MiniMapOverlay.ConfigureMap()`은 같은 mystery floor force reconfigure 시 reveal 상태를 보존하도록 수정되어, 1차 체크포인트(성스러운 돌) 확인 후 미니맵 초기화 문제는 정리됐다.
    - mystery dungeon 몬스터 미등장의 직접 원인 수정은 반영했다.
      - `project.godot`에 `DataManager` autoload를 추가했다.
      - `GameApp.EnsureRuntimeContentReadyAsync()`에서 `DataRegistry` warmup 뒤 `DataManager.LoadAllDataAsync()`까지 강제해, `BuildMysteryDungeonMonsterTemplates()`가 빈 `Monsters` 배열로 끝나지 않도록 했다.
      - headless boot 로그 기준으로 `DataManager Initialized`, `Monsters loaded: 202, maps: 119`까지 확인했다.
    - renderer depth도 같이 보정했다.
      - `DungeonMapRenderer2D`의 row depth / band / offset을 Godot canvas z 범위 안으로 압축해 `p_z > RenderingServer::CANVAS_ITEM_Z_MAX` overflow가 나지 않도록 정리했다.
    - 남은 것은 실제 mystery dungeon 진입 후 `map=903`에서 `monster>0`이 찍히는지와 화면상 등장 확인이다.
  - [~] 7-2-8. mystery dungeon guide discrepancy 재개방
    - 이전 discrepancy log 기준 mystery dungeon down-gate guide는 `map=903 ... guide=0 sacred=0`으로 끝났고, 이번 세션에서 binding fallback과 `guide_data` 우선 좌표 보정을 추가 반영했다.
    - 웹은 gate trigger 좌표 기반으로 guide를 두고 hero overlap에 따라 hide/show하는 loop가 있다.
    - 현재 Godot는 `guide_data 우선 / gate fallback`, `mystery down gate 강제 후보`, `hero overlap hide/show`까지 코드 반영을 마쳤고, 남은 것은 실제 runtime에서 `guide>0`와 시각 노출 확인이다.

- [~] 7-6. combat presentation 확정 차이 재개방
  - level-up 머리 위 gameplay message effect(몬스터 처치 + mystery floor reward), hero runtime 기반 `main hit + sub flare` 막타 연출, 기본공격/스킬/피격/사망/레벨업 combat SFX 배선, death reward delay, damage text frame/offset, expanded condition effect scope는 코드 반영을 마쳤다.
  - hero -> monster common hit fallback도 `maspr:0`가 아니라 web/Classes 기준 `commonspr:2`로 다시 고정했고, C/W 계열은 공용 hit effect를 직접 우선 사용하도록 parity selector를 보강했다.
  - hero active skill 타깃 판정도 `TryResolveFacingMonster(6)` 대신 skill별 rect와 world line sample block 검사 기반으로 교체했다.
  - HUD popup/dialogue blocker, panel/button/quickslot/potion/inventory 계열 마우스 hit-test 안정화, `effect:0` UI 확인음 배선도 이번 턴에 반영했다.
  - 남은 open 항목은 실제 mystery runtime에서 hero -> monster hit layer 체감, hero incoming hit 체감, HUD 패널/대화창 마우스 조작감이 충분히 자연스러운지 수동 검증하는 것이다.


- [x] 7-3. `폐광촌` 창고/상점 NPC 흑백 표시 문제 정리
  - [x] 7-3-1. `폐광촌` 창고/상점 NPC 대상 식별
    - 흑백으로 보이는 NPC의 map 배치, instance id, `npc_res` id, `spriteKey`를 먼저 고정한다.
    - 정상 NPC와 비교할 기준 샘플을 같이 적는다.
  - [x] 7-3-2. NPC runtime binding 경로 감사
    - `GreenfieldWorldRuntime -> TryResolveRuntimeBinding("npc_res:*")` 경로에서 두 NPC가 어떤 binding을 타는지 확인한다.
    - generic binding fallback이 개입하는지 owner-level mapping까지 추적한다.
  - [x] 7-3-3. NPC palette/style/material 원인 분리
    - 흑백 표시가 sprite asset 문제인지, palette/style/material/modulate 적용 문제인지 분리한다.
    - town NPC만의 공통 shader/material 경로가 있는지도 확인 항목에 넣는다.
  - [x] 7-3-4. `폐광촌` NPC owner-level 매핑 규칙 고정
    - 창고/상점 NPC가 공용 generic binding이 아니라 owner-level binding을 타야 한다면 그 규칙을 명시한다.
    - 동일 유형 NPC에 재사용 가능한 매핑 규칙인지도 함께 판단한다.
  - [x] 7-3-5. `폐광촌` NPC 시각 acceptance 고정
    - 창고/상점 NPC가 컬러/팔레트/프레임 상태까지 정상으로 보이는 기준을 수용 기준으로 적는다.
    - 현재 구현 기준:
      - 상점 NPC `instance 566 / npc_res:41 / nspr_2:8`, 창고 NPC `instance 564 / npc_res:236 / nspr_11:7`로 대상 식별을 고정했다.
      - 두 sprite spec의 일부 방향 시퀀스가 실제 `DrawStyle = 5` gray-only frame으로 구성되어 있었고, generic hero-facing 업데이트가 그 방향을 그대로 밟던 것이 직접 원인이었다.
      - 두 NPC는 일부 방향이 gray-only일 뿐 아니라 single-frame stop 상태라서 기본 표시가 `idle`이 아니라 정지처럼 보였다.
      - `GreenfieldWorldRuntime`는 이제 NPC spawn/update 시 gray-only direction을 피하고, 상점/창고 NPC(`564`, `566`)에 한해 single-frame stop 방향도 회피해서 루프 도는 idle 방향을 유지한다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj`로 컴파일 검증을 진행한다.
    - 실제 시각 최종 확인은 `Phase 11` verification으로 넘기고, 구현 작업면은 종료한다.

- [x] 7-4. NPC 행동 규약 재정의 및 parity 복구
  - [x] 7-4-1. NPC 기본 action cycle 상태 머신 정의
    - 평상시 `0/1/2/3` action을 제자리에서 반복하는 기본 상태를 정의한다.
    - 랜덤 순환인지, 순차 순환인지, 프레임 유지 시간 규칙을 같이 정리한다.
    - 현재 구현 기준:
      - `GreenfieldWorldRuntime.ResolveNpcIdleCycleDirection()`은 `상/하/좌/우` 순차 순환을 기본 규약으로 사용한다.
      - `IdleCycleStep`은 spawn 방향에서 시작하고, `NpcTurnIntervalSec` 주기로 다음 방향으로 진행한다.
      - 창고/상점 NPC처럼 gray-only 또는 single-frame stop 방향이 있는 경우는 해당 방향을 건너뛴다.
  - [x] 7-4-2. 히어로 근접 감지 규칙 정의
    - 히어로가 근접 반경에 들어왔을 때만 NPC가 히어로를 바라보도록 반경과 판정 타이밍을 고정한다.
    - 현재처럼 거리 제한 없이 추적하지 않도록 차단 규칙을 명시한다.
    - 현재 구현 기준:
      - `GreenfieldWorldRuntime.UpdateNpcRuntimes()`는 `3타일(Chebyshev)` 근접 범위 안에서만 히어로 방향을 계산한다.
      - 범위 밖에서는 히어로를 더 이상 추적하지 않는다.
  - [x] 7-4-3. 근접 시 회전/복귀 규칙 정의
    - 회전 쿨다운, 시선 유지 시간, 히어로 이탈 후 원위치 복귀 여부를 정한다.
    - 평상시 action cycle과 근접 반응 상태 사이의 전환 조건을 표로 정리한다.
    - 현재 구현 기준:
      - NPC는 범위 밖으로 나간 히어로를 계속 쳐다보지 않고, 다음 업데이트부터 idle cycle로 복귀한다.
      - 회전/복귀 갱신 주기는 기존 `NpcTurnIntervalSec`를 유지한다.
  - [x] 7-4-4. 대화 중 NPC 방향 고정 규칙
    - 대화 시작 시 바라보는 방향, 대화 중 방향 잠금, 대화 종료 후 복귀 규칙을 문서화한다.
    - 상호작용 직전/직후 action cycle 재개 시점도 포함한다.
    - 현재 구현 기준:
      - `BeginNpcDialogue()`에서 대화 대상 NPC는 즉시 히어로 방향으로 회전하고 `DialogueFacingLocked` 상태로 잠긴다.
      - 대화 종료 시 `UnlockNpcDialogueFacing()`이 호출되고, `TurnCooldownSec = 0`으로 즉시 일반 규약(근접 반응 또는 idle cycle)으로 복귀한다.
  - [x] 7-4-5. `폐광촌` NPC 행동 acceptance 고정
    - 일반 NPC, 창고 NPC, 상점 NPC 각각에 대해 평상시 행동과 근접 반응이 올바르게 동작하는 기준을 적는다.
    - 현재 구현 기준:
      - 일반 NPC는 평상시 idle cycle을 유지하고, 히어로가 `3타일(Chebyshev)` 안에 들어오면 그때만 히어로를 바라본다.
      - 창고/상점 NPC는 idle cycle 중에도 gray-only / stop 방향을 피해서 컬러 idle을 유지한다.
      - 대화 시작 시에는 NPC가 히어로를 향해 고정되고, 대화 종료 후에는 즉시 일반 규약으로 되돌아간다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS.

- [x] 7-5. UI 재작성과 사이즈 조절 계획 수립
  - [x] 7-5-1. UI 화면 점유율 측정
    - HUD/status/minimap/quickslot/potion/dialogue/popup/inventory/warehouse/shop/blacksmith/quest 패널의 실제 화면 점유율을 측정한다.
    - 기준 해상도는 `1920x1080`, `1600x900`, `1280x720`으로 고정한다.
    - 현재 구현 기준:
      - 운용 기준 해상도는 `1920x1080`, `1600x900`, `1280x720`로 고정하고, compact breakpoint는 `viewport <= 1366x` 또는 `viewport height <= 760`으로 정리했다.
      - 작은 해상도에서는 외곽 창을 더 넓게 사용하고 내부 카드 minimum size만 줄이는 방식으로 점유율을 제어한다.
  - [x] 7-5-2. 패널별 scroll 정책 분리
    - 어떤 패널은 scroll 허용, 어떤 패널은 scroll 금지인지 매트릭스를 만든다.
    - `popup 전체 스크롤 금지`, `content 영역만 scroll` 원칙을 문서로 고정한다.
    - 현재 구현 기준:
      - `popup`은 전체 카드가 아니라 `_popupScroll` 내부 content만 스크롤한다.
      - `dialogue`도 `_dialogueScroll` 내부 content만 스크롤하고, 카드 외곽 크기는 responsive clamp로 유지한다.
      - `CharacterInventoryOverlay`는 body scroll로 레이아웃을 늘리지 않고, 고정 composition + viewport uniform scale을 기본 정책으로 사용한다.
      - `GameUiDiabloCharacterPanel`은 `CHARACTER` 탭에서 스크롤을 허용하지 않는다. body scroll은 `SPECIALIZATIONS`/`ABILITIES` 탭에서만 허용하고, `CHARACTER`는 clip-only host + panel shell clamp/uniform scale로 처리한다.
  - [x] 7-5-3. 공통 sizing token 추출
    - 외곽 컨테이너 크기와 내부 폰트/아이콘/여백/열 수를 분리하는 공통 token 후보를 뽑는다.
    - `UiMetricScale = 2.0`을 유지하면서도 panel-specific compact preset이 가능하도록 기준을 세운다.
    - 현재 구현 기준:
      - UI 확대는 계속 `UiMetricScale = 2.0` 실치수 계산으로 유지한다.
      - compact mode에서는 외곽 컨테이너를 크게 흔들지 않고, card minimum size / separation / body scroll height만 축소한다.
      - HUD와 inventory overlay 모두 `외곽 컨테이너 유지 + 내부 metric 재배분` 규칙으로 맞춘다.
      - `GameUiDiabloCharacterPanel`의 현재 시각 결과를 공용 UI metric reference로 사용하고, legacy 보정 계층 없이 정규화된 design px + `Ui()/UiSpace()/UiElement()/ResolveFontSize()` 경로만 남긴다.
  - [x] 7-5-4. HUD/minimap safe area budget 정의
    - 좌상단 status/minimap, 하단 quickslot/potion, 중앙 popup/dialogue의 비겹침 예산을 정의한다.
    - viewport 축소 시 어떤 블록이 먼저 compact mode로 내려가는지 우선순위를 정한다.
    - 현재 구현 기준:
      - HUD status card는 compact width/height와 dynamic minimap top offset을 사용해 좌상단 safe area를 유지한다.
      - 중앙 dialogue/popup은 quickslot/hint band 위에서만 배치되고, available width/height 안에서 clamp된다.
      - viewport 축소 시 status/minimap -> dialogue/popup body scroll -> inventory overlay compact 순으로 먼저 압축한다.
  - [x] 7-5-5. popup responsive breakpoint 설계
    - 폭이 충분할 때와 부족할 때의 `2열 -> 1열` 전환 규칙을 정한다.
    - `inventory/equipment/skills/shop/warehouse/blacksmith/quest` 패널별 breakpoint를 분리한다.
    - 현재 구현 기준:
      - HUD popup/dialogue는 `availableWidth/availableHeight` clamp를 기준으로 break되고, 부족한 높이는 body scroll로 흡수한다.
      - CharacterInventoryOverlay는 `1366x/760` 이하에서도 창을 중앙 정렬하고, 내부 재배치 대신 panel 전체를 uniform scale로 줄인다.
  - [x] 7-5-6. merchant/inventory 계열 compact layout 정의
    - 인벤토리/창고/상점/제작/퀘스트 패널의 최소 표시 행/열, detail panel 배치, 텍스트 잘림 규칙을 먼저 문서화한다.
    - 패널별 `anchor/offset/CustomMinimumSize`를 일괄 확장하지 않도록 조정 원칙을 고정한다.
    - 현재 구현 기준:
      - merchant/inventory 계열은 popup 외곽 컨테이너를 확대하지 않고 내부 content scroll과 responsive clamp로 처리한다.
      - CharacterInventoryOverlay는 작은 해상도에서도 웹 inventory panel composition을 유지하고, 부족한 높이는 `_contentScroll`이 흡수한다.
      - 텍스트는 레이아웃 재흐름 대신 `좌측 grid / 우측 detail / 하단 action strip` 구도를 유지한 채 detail 영역에서만 줄인다.
  - [x] 7-5-7. CharacterInventoryOverlay compact mode 설계
    - 작은 해상도에서도 inventory reference 구도를 무너뜨리지 않는 축소 규칙을 정한다.
    - 현재 구현 기준:
      - CharacterInventoryOverlay는 웹 `dungeon-neko-web/src/components/overlay/panels/core/InventoryPanel.tsx`를 정본으로 삼아 `상단 title/close`, `상단 소지수/필터`, `좌측 6x5 grid`, `우측 detail`, `하단 primary action + selection strip` 레이아웃으로 재작성했다.
      - 필터 버튼은 overlay 내부 상태로 관리하되, 분류 기준은 `GameScene` item catalog(`MainType`, encoded option line) 해석으로 맞춘다.
      - `판매/보관`은 기능 회귀를 막기 위해 DETAIL 패널 하단 보조 액션으로 유지하고, 주 액션은 하단 `장비 장착/해제` 버튼으로 고정한다.
      - viewport 축소 시에도 외곽 창은 중앙 정렬을 유지하고, overflow 대신 `_contentScroll`이 내부 높이를 흡수한다.
  - [~] 7-5-7a. hero equipment sprite 7슬롯 parity
    - [x] 7-5-7a-1. hero visual swap 모델 확장
      - `HeroNativeClipSwap`, `HeroNativeVisualSwapSet`를 추가했다.
      - `HeroVisualEquipmentState`는 이제 `Cap/Body/Glove/Boots/Weapon` source와 `HA_1 body(0..10) / glove(11..22)` clip override 세트를 함께 만든다.
    - [x] 7-5-7a-2. body/glove fallback 규칙 정리
      - `EquipmentService.BuildHeroVisualEquipmentState()`를 웹 기준으로 맞춰 `body=slot1 ?? slot4`, `glove=slot4 ?? slot1` 규칙을 반영했다.
      - `boots=slot6`, `weapon=slot3`, `cap=slot0` 기존 역할은 유지한다.
    - [x] 7-5-7a-3. runtime clip swap resolve 경로 추가
      - `DungeonSprite2D`는 `imageSourceIndex + clipIndex` 기준 hero clip swap을 track swap보다 우선 적용하도록 확장됐다.
      - `DungeonRenderEntry2D`, `GreenfieldWorldRuntime`에도 clip swap count / apply entry가 추가됐다.
    - [~] 7-5-7a-4. GameScene snapshot apply closeout
      - 현재 `GameScene`에는 `ResolvePlayerHeroVisualSwaps()`가 추가됐지만, `GameScene.PlayerState`는 아직 track-only apply 경로를 사용한다.
      - 따라서 clip swap end-to-end 적용 closeout은 아직 남아 있다.
    - [~] 7-5-7a-5. build gate
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` FAIL.
      - 실패 원인: `/Users/ethan/Workspace/dev/DungeonNeko/DungeonNeko-Godot/scripts/ui/GameUiManager.cs(127,13)`에서 `GameUiCharacterPanel` 형식을 찾지 못함 (`CS0246`).
      - 현재 워크트리 기준 `DungeonNeko-Godot/scripts/ui/GameUiCharacterPanel.cs` 삭제 상태가 blocker로 보인다.
  - [~] 7-5-8. `O` 패널(diablo inventory) 레퍼런스 재작성
    - 기준 정본은 `docs/opanel.png` 하나로 고정한다.
    - 입력/진입 경로는 유지한다.
      - `ui_diablo_inventory` -> `GameUiManager` -> `GameUiCharacterPanel`
      - `ui_equipment`는 `CHARACTER`, `ui_skills`는 `ABILITIES` 탭 전환으로 흡수한다.
    - 구조 acceptance:
      - `GameUiCharacterPanel`은 기존 inventory overlay 레이아웃을 버리고 `상단 탭 / 좌측 프로필 / 우측 상단 hero preview / 장비 슬롯 프레임 / 우측 하단 인벤토리`로 재작성한다.
      - `CHARACTER / SPECIALIZATIONS / ABILITIES` 3탭 모두 실제 콘텐츠를 가진다.
      - `CHARACTER` 기본 오픈, 닫았다 다시 열면 항상 `CHARACTER`로 초기화한다.
    - 데이터 acceptance:
      - `CHARACTER`는 플레이어 이름, `칭호 없음`, 실데이터 기반 기본 스탯 요약, 현재 hero preview, 실제 8개 장비 슬롯, 하단 `Equipment/Consumables/Quest/Aspects` 필터를 사용한다.
      - `SPECIALIZATIONS`는 새 특성 시스템이 아니라 `PlayerSnapshot + HeroCombatStatCalculator.Build(snapshot, null, false)` 기반 전투 프로필 탭으로 구현한다.
      - `ABILITIES`는 `HeroSkillCatalog` 6개 스킬과 현재 퀵슬롯 바인딩 상태를 같은 패널 안에서 보여주고 등록/해제 액션을 제공한다.
    - 구현 메모:
      - `GameUiCharacterPanel`에 `SetQuickSlots()`와 스킬 퀵슬롯 bind/clear event를 추가한다.
      - 중앙 캐릭터는 paperdoll이 아니라 `DungeonSpritePreviewRenderer2D` 기반 hero job0 preview를 사용한다.
      - 레퍼런스 초과 장비 프레임 2칸은 비활성 장식 슬롯으로 둔다.
    - 2026-03-11 구현 메모:
      - 구형 `GameUiCharacterPanel`은 제거했고, `GameUiDiabloCharacterPanel`을 새 `O` 패널 구현체로 추가했다.
      - 새 패널은 기존 `6x5 inventory + detail` 구조 대신 `상단 CHARACTER/SPECIALIZATIONS/ABILITIES`, 좌측 프로필, 우측 상단 hero preview + 장비 슬롯 프레임, 우측 하단 inventory deck 구조를 사용한다.
      - `SPECIALIZATIONS`는 `HeroCombatStatCalculator.Build(snapshot, null, false)` 기반 파생 전투 수치/속성 프로필 뷰로 구현했다.
      - `ABILITIES`는 `HeroSkillCatalog` 6개 스킬과 현재 퀵슬롯 바인딩 상태를 패널 내부에서 보여주고 bind/clear 이벤트를 `GameUiManager` 기존 이벤트에 다시 연결했다.
      - `GameUiManager.HandleGlobalHotkeys()`는 `O` 패널이 열린 상태에서 `ui_equipment` -> `CHARACTER`, `ui_skills` -> `ABILITIES` 탭 전환으로 소비하도록 수정했다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS (`경고 0 / 오류 0`)
    - 2026-03-11 후속 수정 메모:
      - `GameUiHeroPreviewState`를 추가하고 `GameScene -> GameUiCommandContext -> GameUiManager -> GameUiDiabloCharacterPanel` 경로로 현재 hero visual swap/action/direction/render mode를 전달하도록 정리했다.
      - `DungeonSpritePreviewRenderer2D`는 이제 hero visual swap 적용과 preview framing(`anchorY/scale`) override를 지원한다.
      - `GameUiDiabloCharacterPanel`의 character preview는 더 이상 고정 `hero_job:0` 외형만 쓰지 않고, runtime visual swap을 적용한 현재 hero 외형을 사용한다.
      - preview shell을 `268x320`로 키우고 `anchorY=0.82`, `scale=1.35` framing으로 박스 점유율을 올렸다.
      - 하단 selection summary strip을 제거했고, 패널은 full-width가 아니라 우측 도킹 + 폭 clamp(`760~900`)로 배치하도록 수정했다.
      - inventory는 hero preview 아래 우측 컬럼으로 재배치했고, `6x5` grid, slot `72px`, icon `16px`(warehouse 기준 `18`에 가장 가까운 8배수), shell `1040x880`, actual capacity `30` 기준으로 다시 축소했다.
      - `I` 패널은 scaled size 기준 viewport clamp를 적용했고, header drag로 위치 이동이 가능하도록 정리했다.
      - character stage는 절대좌표 중심 배치 대신 `좌 슬롯열 / 중앙 preview / 우 슬롯열` 3열 레이아웃으로 재구성했고, 좌측 profile/stat 패널은 `192x820` design px, 우측 컬럼은 `472` design px 폭으로 고정했다. 하단 unused 슬롯 3개는 제거했다.
      - inventory 하단 row는 `GOLD`만 유지하도록 정리했고, `30/30` badge는 제거했다. item action은 하단 버튼 대신 선택 슬롯 기준 고정 tooltip(`장착/해제/버리기`)으로 이동했다.
      - `버리기`는 tooltip 내부 `예/아니오` 확인을 거친 뒤 `DiscardInventoryItem` 경로로 실제 inventory stack을 삭제하도록 연결했고, pinned tooltip은 바깥 클릭이나 우상단 `X`로 닫을 수 있게 했다.
      - 상단 `CHARACTER / SPECIALIZATIONS / SKILLS`와 inventory filter `전체/장비/소모/퀘스트/기타` 모두 attached tab strip으로 재작성했고, active red / inactive stone palette + bottom border 제거로 선택 상태를 즉시 구분할 수 있게 보정했다.
      - window base height는 `908`, window outer margin top/bottom은 `8`, frame separation은 `2`, character content top inset은 `1`로 줄여 header 아래 죽는 공간을 회수했다.
      - `전투속성`은 `배너 + 핵심 전투 수치 + 장비/유틸 보정 + 속성 공격 프로필` 구성으로, `스킬`은 `목록 + 선택 스킬 요약 + 메트릭 + 설명 + 퀵슬롯 바인딩` 구성으로 다시 재배치했다.
      - 기본 인벤토리 capacity는 `InventoryService.InventorySlotLimit = 30` 기준으로 통일했고, save normalize/reward/shop/warehouse 흐름도 같은 기준을 사용한다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS (`경고 0 / 오류 0`)
    - 2026-03-11 전투속성/스킬 탭 안정화 후속 메모:
      - `GameUiDiabloCharacterPanel`의 `SPECIALIZATIONS`/`ABILITIES` body는 `ScrollContainer` 기반으로 바꿨고, compact breakpoint는 `1366x/760` 규칙으로 고정했다.
      - `CHARACTER` 탭은 scroll 대상에서 제외한다. `_tabViewport`는 clip-only host로 두고, 내부 `ScrollContainer`는 `SPECIALIZATIONS`/`ABILITIES`에서만 생성한다.
      - `전투속성` 탭은 card grid 대시보드로 재작성해 `배너 / 핵심 전투 수치 / 장비·유틸 보정 / 속성 프로필` 4블록을 body scroll 안에서 재흐름하도록 정리했다.
      - `스킬` 탭은 `페이지 1 / 페이지 2` 12칸 quickslot 매트릭스를 동시에 보여주고, 같은 스킬의 다중 바인딩 슬롯을 모두 표시한다.
      - 스킬 bind/clear는 절대 슬롯 인덱스 `0..11` 기준으로 이벤트를 올리도록 수정했다.
      - panel 전용 quickslot payload를 위해 `GameScene`, `GameUiSnapshot`, `GameUiSnapshotFactory`, `GameUiSyncCoordinator`, `GameUiManager` 경로에 `AllQuickSlots` 전달이 추가됐다.
      - 2026-03-12 scale 단일화로 `LegacyPanelMetricBaseline`, `PanelPx`, `PanelPxFloat`, `PanelSize`, `SetPanelAnchoredRect`, `LegacyFontScale`를 제거했고, 같은 출력은 정규화된 design px와 공용 `UiScaleMetrics` 상수로 유지한다.
      - 최종 `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS (`경고 0 / 오류 0`)
    - 2026-03-11 입력/merchant/dialogue 정리 메모:
      - `project.godot`에서 `ui_diablo_inventory`를 `I`로 옮기고, `ui_inventory/ui_equipment/ui_skills` 입력 정의는 제거했다.
      - `GameUiManager.HandleGlobalHotkeys()`와 popup routing은 이제 `shop/warehouse/blacksmith/quests`만 generic panel 경로를 사용한다.
      - popup toolbar는 `상점/창고/제작/퀘스트`만 남기고, `shop/warehouse/blacksmith/quests`는 공통 diablo-style shell로 다시 렌더링한다.
      - NPC dialogue card도 하단 위치 유지 + diablo-style header/body/button 톤으로 재작성했고, 좌측 portrait + 이름 / 우측 대사 본문 구조로 다시 정리했다.
      - dialogue/popup 외곽 크기도 내용이나 viewport 비율에 따라 자라지 않도록 fixed shell 기준으로 다시 고정했다.
      - follow-up으로 dialogue는 `header/content/footer` 3단 레이아웃으로 다시 절개했고, NPC 이름 중복을 제거해 header 한 곳에만 남겼다.
      - portrait preview는 small slot + transparent background + fallback glyph로 바꿨고, preview framing anchor도 올려 바닥에 붙어 보이던 인상을 줄였다.
      - choice button은 footer 전폭 세로 스택으로 옮기고, guide label도 footer 하단에 고정했다. responsive scroll height는 footer/header 실높이를 뺀 본문 영역 기준으로 다시 계산한다.
      - 추가 보정으로 NPC portrait는 이중 panel을 제거하고 단일 좌측 card로 정리했다. 좌측 card 높이는 우측 text card와 같은 `contentHeight`를 쓰고, preview 기본 action state는 `1`을 우선 선택한다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS (`경고 0 / 오류 0`)
  - [~] 7-5-8. mystery dungeon reward popup web parity 고정
    - 기준 정본은 `dungeon-neko-web/src/components/overlay/panels/PopupOverlay.tsx`, `GameOverlay.tsx`, `DungeonFlowController.ts`로 고정한다.
    - 시각 acceptance:
      - reward popup은 `docs/dpopup.png` / web `PopupOverlay` 실측 기준 `496x364` 전용 노드 트리(`DungeonRewardPopupView`)로 그리고, viewport에는 uniform scale만 적용한다
      - 기존 범용 `_popupCard` / `dungeonReward` 분기 / 구형 reward popup builder는 제거한다
      - `reward chip -> message -> monster -> item -> confirm` 순서 유지
      - monster/item은 내부 section scroll 없이 popup height 확장으로 수용
      - 몬스터 preview는 `56px` 디자인 기준 + reward popup 전용 transparent viewport로 렌더링한다
    - 행동 acceptance:
      - `popupMode=Off`면 popup만 숨기고 floor apply 후 바로 play
      - `popupMode=ThreeSec`면 `AutoCloseMs=3000`
      - `InventoryFull` 경고는 중앙 message가 아니라 `획득 아이템` section 내부 notice에만 표시한다
    - 2026-03-11 구현 메모:
      - `DungeonNeko-Godot/scripts/ui/DungeonRewardPopupView.cs` 추가
      - `HudController`는 새 전용 뷰 show/hide만 담당하도록 교체
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS (`경고 0 / 오류 0`)
    - 2026-03-11 후속 보정:
      - `GameUiRewardPanel`에서 section `ScrollContainer`를 제거하고 row 수 기준으로 popup height를 늘리도록 조정
      - `DungeonSpritePreviewRenderer2D.TransparentBackground`를 reward popup 몬스터 프리뷰에만 적용해 회색 배경 제거
      - 몬스터 프리뷰 anchor/scale과 cell size를 키워 `docs/dpopup.png`의 `처치 몬스터` 영역에 더 가깝게 보정
      - `ItemSectionNotice` 필드를 추가해 bag-full warning을 `획득 아이템` section 내부로 이동
      - `ui_menu`는 항상 닫힘, `ui_ok/action_attack`은 `CloseOnPrimaryAction`일 때만 닫힘
      - floor 이동은 `하강 -> loading/apply -> reward popup -> popup 닫힘 후 play` 순서
    - 검증 해상도는 `1920x1080`, `1600x900`, `1280x720`로 고정한다.
    - 창 자체는 `640x480` 기준으로 키우고, 작은 해상도에서는 uniform scale로만 줄인다.
    - 현재 구현 기준:
      - 작은 해상도에서는 overlay window를 우측 고정이 아니라 중앙 정렬로 배치한다.
      - 상단/상세 card minimum size, separation, scroll height만 줄여 내부 레이아웃을 compact하게 재배치한다.
      - 전체 창 확대보다 내부 body scroll 우선 원칙을 적용한다.
  - [x] 7-5-8. UI sizing acceptance 고정
    - `가로 잘림 0`, `불필요한 세로 스크롤 0`, `HUD/미니맵 겹침 0`, `1280x720 기본 조작 가능`을 acceptance로 고정한다.
    - 현재 구현 기준:
      - HUD/minimap 겹침은 status card compact + minimap dynamic offset으로 방지한다.
      - popup/dialogue/inventory overlay는 외곽 카드보다 body scroll을 우선 사용해 작은 해상도에서도 조작 가능 상태를 유지한다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS.
  - [x] 7-5-9. item/stats parity를 web 기준으로 고정
    - 기준 정본은 `dungeon-neko-web/src/components/overlay/OverlayPrimitives.tsx`, `dungeon-neko-web/src/game/scenes/main/combat/HeroCombatProfileBuilder.ts`, `dungeon-neko-web/src/game/scenes/main/progression/ServerItemRollService.ts`, `dungeon-neko-web/src/game/scenes/main/progression/ProgressionService.ts`로 고정한다.
    - 데이터 acceptance:
      - `ItemSaveState`는 `Stat`, `StatType`, `EquipOptions[6]`, `Identity`, `EquipOptionCnt`를 저장한다.
      - `ItemSnapshot`은 icon/type/stat/equipOption/identity/costume/price/stackable까지 직접 싣는다.
      - `LocalProgressAdapter`는 generated item의 equip option/identity/option count를 왕복 시 재추정하지 않는다.
    - 규칙 acceptance:
      - item 생성/드롭/층 보상은 base options와 rolled equip options를 분리 저장한다.
      - item 이름 색상, base stat line, encoded equip option line, palette-only icon 후보, detail text는 web 규칙과 같은 helper를 사용한다.
      - `HeroCombatStatCalculator`는 synthetic budget이 아니라 실제 장착 아이템의 `stat/statType/options/equipOptions/plus/socket`을 기준으로 계산한다.
      - `addstat/resetstat` 후 자원 반영은 web 규칙(`VIT=maxHp만`, `INT=maxMp+현재mp`, reset은 base stat 기준 초기화)을 따른다.
    - UI acceptance:
      - `I` 패널 캐릭터 탭 기본 능력치는 `스탯명 ... [+] ... 값` 순서를 사용한다.
      - `statPoint > 0`일 때만 `+` 버튼을 노출하고, `0`일 때는 같은 폭 spacer로 정렬을 유지한다.
      - `PNT` 강조 행과 `스탯 초기화` 버튼을 같은 블록에 표시한다.
      - `addstat_not_enough_stat_point`는 한국어 사용자 메시지로 노출한다.
    - 2026-03-11 구현 메모:
      - `ApiSnapshots`, `GameSaveState`, `LocalProgressAdapter`, `ItemCatalogService`, `ApplicationLocalApiFacade*`를 확장해 per-item presentation 필드와 legacy save fallback을 반영했다.
      - `ItemUiFactory`를 item presentation 공용 SoT로 추가했고, `GameScene`/`GameUiManager`/`GameUiDiabloCharacterPanel`이 이를 사용하도록 연결했다.
      - `GameUiDiabloCharacterPanel`은 기본 스탯 줄에 `+` 버튼, `PNT`, `스탯 초기화` 액션을 추가해 실제 `AddStatRequested`/`ResetStatRequested`를 emit한다.
      - `HeroCombatStatCalculator`와 `GameScene.PlayerState`는 web item-driven 계산/이전 snapshot 기반 자원 보존으로 교체했다.
      - `dotnet build DungeonNeko-Godot/DungeonNeko.csproj` PASS (`경고 0 / 오류 0`).

## Phase 8. 전투 로직 수정 / 몬스터 이펙트

- 관련 문서:
  - `dev/active/godot-phase8-combat/godot-phase8-combat-plan.md`
  - `dev/active/godot-phase8-combat/godot-phase8-combat-context.md`
  - `dev/active/godot-phase8-combat/godot-phase8-combat-tasks.md`
- 작업 지시:
  - `Phase 8`의 세부 계약, acceptance, fixture/checklist는 위 3문서를 정본으로 사용한다.
  - `godot-porting` 쪽에는 closeout 상태만 유지한다.
- [x] 8-1. 전투 상태 계약 재잠금
- [x] 8-2. active skill hit timeline 재구성
- [x] 8-3. monster combat runtime 정리
- [x] 8-4. combat presentation / audio 이벤트 파이프라인 정리
- [x] 8-5. monster effect runtime 정리
- [x] 8-6. 검증 게이트 초안 작성

## Phase 9. 리팩토링

- 관련 문서:
  - `dev/active/godot-refactor/godot-refactor-plan.md`
  - `dev/active/godot-refactor/godot-refactor-context.md`
  - `dev/active/godot-refactor/godot-refactor-tasks.md`
  - `dev/active/godot-refactor-game-scene/godot-refactor-game-scene-plan.md`
  - `dev/active/godot-refactor-game-scene/godot-refactor-game-scene-context.md`
  - `dev/active/godot-refactor-game-scene/godot-refactor-game-scene-tasks.md`
- 작업 지시:
  - `Phase 9`를 시작할 때는 항상 위 문서를 먼저 읽는다.
  - boundary/runtime/legacy cleanup은 `godot-refactor` 문서 세트를 정본으로 사용한다.
  - `GameScene` 후속 분해는 `godot-refactor-game-scene` 문서 세트를 정본으로 사용한다.
  - `godot-porting` 쪽에는 진입점과 현재 우선순위만 짧게 유지한다.
- [x] 9-1. production/test boundary split
  - 실제 세부 작업과 acceptance 갱신은 `godot-refactor-tasks.md`의 `Production/Test Boundary Split` 섹션에서 진행한다.
- [x] 9-2. `GameScene` / HUD 분해
  - `godot-refactor` 본 트랙과 `godot-refactor-game-scene` follow-up 트랙까지 반영 완료했다.
- [x] 9-3. runtime/resource port 경계 도입
  - 실제 세부 작업과 acceptance 갱신은 `godot-refactor-tasks.md`의 `Runtime and Resource Ports` 섹션에서 진행한다.
- [x] 9-4. legacy cluster cleanup
  - 실제 세부 작업과 acceptance 갱신은 `godot-refactor-tasks.md`의 `Legacy Cluster Cleanup` 섹션에서 진행한다.

## Phase 10. 임시 공간

- [ ] reserved

## Phase 11. Verification Gates

- [ ] Gate 0. build / preflight baseline 재구성
  - `dotnet build`를 기본 엔트리로 유지하고, 이후 replay/frame/perf 검증용 실행 스크립트를 다시 정의한다.
- [ ] Gate 1. source coverage closure
  - `Classes/*` 기준 owner 없는 legacy 책임 0건 상태를 다시 고정한다.
- [ ] Gate 2. resource coverage closure
  - sprite/object/map 자산의 runtime missing/fallback을 검증 가능한 항목으로 다시 수집한다.
- [ ] Gate 3. scenario replay diff
  - town -> UI/HUD -> quest/shop/warehouse/blacksmith -> dungeon 루프를 deterministic diff 세트로 만든다.
- [ ] Gate 4. golden frame diff
  - title/town/HUD/minimap/dialogue/inventory/dungeon frame fixture를 다시 정의한다.
- [ ] Gate 5. 성능/메모리 baseline
  - cold boot / town / dungeon entry / dungeon return 구간 측정 규약을 고정한다.

## Phase 12. 리소스 저장/로딩 전환 / Selective Prebake

- [ ] hero/monster/effect 자산 저장 포맷 규약 재정의
- [ ] `DataRegistry` 중심 로딩 경로의 Godot native resource loading 치환 범위 설계
- [ ] `SpriteFramesPrebakeJob` selective prebake 범위 재정의
- [ ] effect 유형별 runtime/material/resource 책임 경계 정리
- [ ] hot path 기준 pre-bake / precomposed cache 측정
- [ ] Godot editor에서 frame/action 확인 가능한 asset layout 확정
- [ ] runtime parity와 asset pipeline 책임 경계 문서화

## 현재 검증 엔트리

- `dotnet build DungeonNeko-Godot/DungeonNeko.csproj`
